version: '2'
services:
  sensors:
    extends:
        file: common-services.yml
        service: common
    image: migruiz/clima-sensors-zero
    container_name: clima-sensorsttt
    restart: always
    privileged: true
    
